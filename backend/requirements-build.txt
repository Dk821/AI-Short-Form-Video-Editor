# Extra tooling needed only to produce the Windows desktop build.
# Install into the same venv as requirements.txt:
#     venv\Scripts\activate
#     pip install -r requirements.txt -r requirements-build.txt
-r requirements.txt
pyinstaller==6.11.1
